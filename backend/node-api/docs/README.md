# Documentation location

Engineering documentation for this NestJS service has moved to **[`../../docs/README.md`](../../docs/README.md)** (repository folder **`backend/docs/`**).

Runbooks, API notes, and architecture docs are maintained there so they stay stable when additional backends (for example `java-api/`) are added next to `node-api/`.
